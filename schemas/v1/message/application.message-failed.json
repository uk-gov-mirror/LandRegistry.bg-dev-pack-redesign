{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "description": "JSON schema to notify that a message has failed to be added to an application.",
  "properties": {
    "data": {
      "$ref": "#/$defs/payload_data"
    }
  },
  "required": ["data"],
  "$defs": {
    "payload_data": {
      "type": "object",
      "properties": {
        "failure_reason": {
          "$ref": "#/$defs/failure_reason"
        }
      },
      "required": ["failure_reason"]
    },
    "failure_reason": {
      "type": "string",
      "description": "Reason why the message attachment failed."
    }
  }
}